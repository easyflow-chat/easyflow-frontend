import { FunctionComponent, PropsWithChildren } from 'react';

const ModalContent: FunctionComponent<PropsWithChildren> = ({ children }): JSX.Element => {
  return <>{children}</>;
};

export default ModalContent;
