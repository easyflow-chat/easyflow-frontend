import { FunctionComponent, PropsWithChildren } from 'react';

const ModalTrigger: FunctionComponent<PropsWithChildren> = ({ children }): JSX.Element => {
  return <>{children}</>;
};

export default ModalTrigger;
